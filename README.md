
  # VIT Placement Simulator Game

  This is a code bundle for VIT Placement Simulator Game. The original project is available at https://www.figma.com/design/Y5X23r6eZ8EdxT2zJUd3Bm/VIT-Placement-Simulator-Game.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  